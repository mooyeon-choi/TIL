#include <iostream>

using namespace std;

int N, S, numbers[20];
bool used[20] = { false, };

int main() {
	cin >> N >> S;
	for (int i = 0; i < N; i++) cin >> numbers[i];
	for (int i = 0; i < 1 << N; i++) {
		for (int j = 0; j < N; j++) {

		}
	}
}